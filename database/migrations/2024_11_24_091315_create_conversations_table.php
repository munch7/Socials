<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateConversationsTable extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('conversations', function (Blueprint $table) {
            $table->id();
            $table->string('facebook_id')->unique(); // Facebook conversation ID
            $table->text('snippet')->nullable();    // Message preview
            $table->json('participants')->nullable(); // List of participants
            $table->timestamp('last_activity')->nullable(); // Timestamp of the last activity
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('conversations');
    }
}
